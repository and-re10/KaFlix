<div class="conatiner m-0 p-0">
    <button class="btn btn-primary ml-5">Adicionar um video</button>
    <div class="row">
        {{-- @foreach ($myVideos as $video)
            <div class="col-md-3">
                {{$video}}
            </div>
        @endforeach --}}
    </div>
</div>